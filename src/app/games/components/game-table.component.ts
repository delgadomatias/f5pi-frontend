import { ChangeDetectionStrategy, Component, input } from '@angular/core';

@Component({
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [],
  selector: 'f5pi-game-table',
  styles: [``],
  template: ``,
})
export class GameTableComponent {
  game = input.required<
}